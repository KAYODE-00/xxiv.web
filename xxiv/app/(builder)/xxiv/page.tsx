/**
 * Base route for XXIV editor
 * URL: /xxiv
 *
 * The XXIVBuilder component is now rendered in layout.tsx to persist
 * across route changes. This prevents remounts and duplicate API calls.
 *
 * Collaboration features (RealtimeCursors, ActivityNotifications) are
 * integrated in XXIVBuilderMain.tsx.
 */
export default function XXIVEditorRoute() {
  return null;
}